  o Minor features (sandbox):
    - Permit the unlinkat(), newfstatat(), and ppoll() system calls: they seem
      to be required by some ARM64 glibc implementations.
      Addresses part of ticket 24454.
